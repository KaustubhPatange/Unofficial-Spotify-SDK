name: build
on: [push]
jobs:
  job_id:
    runs-on: windows-latest
    steps:
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Build Gradle
        run: ./gradlew build
